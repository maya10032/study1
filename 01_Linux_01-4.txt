① 「03_Linux」内に「vimTest」ディレクトリを新規作成と同時に中に入る(ワンライナーコマンドで実行)。
② その中に「test.txt」という新規ファイル名でVimを起動する。
③ 文字を「あいうえお」と入力し:を使った保存終了をする。
④ 再度Vimを既存のファイルで起動して「かきくけこ」を改行後に追記し、省略コマンドを使用した保存終了をする。
⑤ コンソール上からファイルの内容を簡易表示し、入力した文字を確認する。

$ mkdir vimTest && cd $_：ディレクトリ作成後移動
